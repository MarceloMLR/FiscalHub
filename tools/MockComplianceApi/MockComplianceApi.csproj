<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- Mock de dev da API de compliance (estilo Avalara). NÃO é parte do produto;
       existe só para exercitar o fluxo assíncrono ponta a ponta sem a plataforma real. -->
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
